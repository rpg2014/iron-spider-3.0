export enum EC2State {
  pending = 0,
  running = 16,
  shuttingDown = 32,
  terminated = 48,
  stopping = 64,
  stopped = 80,
}

export enum Status {
  Pending = 0,
  Running = 16,
  ShuttingDown = 32,
  Terminated = 48,
  Stopping = 64,
  Stopped = 80,
}
