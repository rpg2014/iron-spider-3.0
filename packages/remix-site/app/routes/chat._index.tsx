import { Link } from "@remix-run/react";
import { Button } from "~/components/ui/Button";

/**
 * Centered box with a column of full width buttons to the different chat types, summarize, agent.
 * @returns
 */
export default function Index() {
  return <p className="m-2">Click on a nav item above</p>;
}
